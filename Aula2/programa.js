console.log("=== MEU PRIMEIRO PROGRAMA ===");

// Entrada (Dados) - capturar dados do seu usuário
let nome = prompt("Insira seu nome:");

// Processamento (Dados)
nome = nome.toUpperCase();

// Saída (Dados)
document.write(nome);